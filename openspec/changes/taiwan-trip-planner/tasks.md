## 1. Spike 與決策關卡

此組必須在寫任何引擎程式碼之前完成。design.md D3 的回退決策取決於本組結果。

- [x] 1.1 申請 TDX 帳號與 API 金鑰，以指令稿成功取得 access token 並呼叫一支觀光 POI API，驗證回傳 HTTP 200 與非空資料
- [x] 1.2 測試 TDX 旅運規劃模組於三種情境（本島市區、本島偏鄉、離島）的涵蓋率，記錄每種情境是否回傳含班次的方案，產出一份 spike 記錄檔
- [ ] 1.3 量測 TDX 觀光 POI 資料中營業時間欄位的缺漏比例（抽樣至少三個縣市、八大類別各一），將數字寫入 spike 記錄檔
- [x] 1.4 依 1.2 結果做出 D3 決策：採用 TDX 旅運規劃模組，或回退至以原始時刻表自行實作銜接判定。將決策與理由寫入 spike 記錄檔，並在決策為回退時更新 design.md 的 D3
- [x] 1.5 確認 Vercel free tier 的函式執行時間上限數值，寫入 spike 記錄檔

## 2. 專案骨架

- [x] 2.1 初始化 Next.js + TypeScript 專案，驗證 `npm run dev` 可啟動且首頁可載入
- [x] 2.2 設定測試框架，驗證一個示範測試可執行並通過
- [x] 2.3 建立環境變數設定與型別化讀取，驗證缺少必要金鑰時啟動即失敗並顯示明確訊息
- [x] 2.4 加入金鑰洩漏防護檢查，驗證建置後的 client bundle 中不含任何 TDX 或 LLM 金鑰字串

## 3. Provider 介面與 TDX 實作

- [x] 3.1 定義 `PoiProvider` 與 `RoutingProvider` 介面型別，驗證型別檢查通過且介面涵蓋 specs 中所有需回傳的欄位
- [ ] 3.2 實作 TDX 認證與具重試的 HTTP 客戶端，驗證 token 過期時能自動換發的測試通過
- [ ] 3.3 實作 TDX 的 `PoiProvider`：依區域與類別取候選、依座標取周邊、地理編碼，驗證八大類別各自的對應測試通過
- [ ] 3.4 實作帶 TTL 的 POI 快取（design D6），驗證同一區域類別的重複查詢只發出一次外部請求的測試通過
- [x] 3.5 實作台灣本島與離島範圍的座標判定，驗證範圍內外的邊界測試通過（含澎湖、金門、馬祖、蘭嶼、綠島）

## 4. 附近搜尋（capability: nearby-search）

- [ ] 4.1 實作周邊查詢 API route，驗證八大類別查詢皆回傳含名稱、類別、座標、距離的結果
- [ ] 4.2 實作結果依距離遞增排序與缺漏欄位標註，驗證對應 scenario 測試通過
- [ ] 4.3 實作地點名稱解析與多筆相符時的候選回傳，驗證「地點名稱有多個相符結果」scenario 測試通過
- [ ] 4.4 實作查詢中心超出台灣範圍的拒絕行為，驗證「查詢台灣範圍外」scenario 測試通過
- [ ] 4.5 實作查無結果的明確回報，驗證「偏遠地區查無結果」scenario 測試通過
- [ ] 4.6 實作附近搜尋頁面，含文字輸入與取得目前位置兩種方式，驗證定位成功與定位失敗兩條路徑於瀏覽器中皆表現正確

## 5. 交通銜接（capability: transit-routing）

- [ ] 5.1 依 1.4 的決策實作 TDX 的 `RoutingProvider`，驗證查詢回傳含實際發車與抵達時間的方案
- [ ] 5.2 實作班次分段的資訊完整性，驗證公車分段含路線代號與上下車站牌、步行分段含距離與時間的測試通過
- [ ] 5.3 實作時間窗可行性判定，驗證可行與不可行兩個 scenario 測試通過
- [ ] 5.4 實作查無方案時的明確回報，驗證「末班車已過」scenario 測試通過且回傳中不含任何估算值
- [ ] 5.5 實作近距離的純步行方案，驗證「近距離起訖點」scenario 測試通過

## 6. 排程引擎（capability: trip-planning）

- [ ] 6.1 定義行程的資料結構（站點、轉場、每日時間窗），驗證型別檢查通過
- [ ] 6.2 實作第一步候選召回，驗證單次規劃的 TDX 呼叫次數不超過「區域數 × 類別數」的測試通過
- [ ] 6.3 實作第二步地理分群，驗證分群結果不會將距離過遠的點排入同一天的測試通過
- [ ] 6.4 實作第三步 LLM 選點，輸入為候選 ID 清單、輸出為排序後的 ID 清單，驗證 LLM 無法回傳候選集合外 ID 的測試通過
- [ ] 6.5 實作 LLM 結構化輸出的 schema 驗證，驗證格式不符時視同該步失敗並計入重試上限的測試通過
- [ ] 6.6 實作第四步可行性驗證：營業時間、轉場銜接、每日總時長，驗證三個對應的 scenario 測試各自通過
- [ ] 6.7 實作驗證失敗時退回第三步的重試迴圈與次數上限，驗證超過上限時回報無可行行程而非無限重試的測試通過
- [ ] 6.8 實作無可行行程時的原因說明，驗證「時間窗過短」scenario 測試通過
- [ ] 6.9 實作營業時間缺漏時的標註行為，驗證「營業時間資料缺漏」scenario 測試通過
- [ ] 6.10 實作台灣範圍外請求的拒絕，驗證「請求台灣以外的目的地」與「請求離島行程」兩個 scenario 測試通過

## 7. 對話式輸入

- [ ] 7.1 實作自然語言需求抽取，驗證「必要欄位齊全」scenario 測試通過且抽取結果符合預期欄位
- [ ] 7.2 實作缺漏欄位的追問，驗證「必要欄位缺漏」scenario 測試通過且在取得答覆前不產出行程
- [ ] 7.3 實作客戶端持有對話歷程的無狀態規劃 API（design D5），驗證多輪對話後仍能正確產出行程的測試通過
- [ ] 7.4 實作以目前位置作為出發地，驗證定位成功與定位失敗兩個 scenario 於瀏覽器中表現正確
- [ ] 7.5 實作規劃進度的串流回報（design 風險項），驗證長時間規劃不會因函式逾時而失敗

## 8. 帳號與行程儲存（capability: trip-library）

- [ ] 8.1 建立 Supabase 專案與行程資料表結構，驗證可自應用程式讀寫一筆測試資料
- [ ] 8.2 實作第三方登入，驗證「登入成功」與「使用者取消授權」兩個 scenario 表現正確
- [ ] 8.3 實作未登入亦可完整規劃與搜尋，驗證「未登入使用者規劃行程」scenario 測試通過
- [ ] 8.4 實作未登入者按下儲存時引導登入並保留待儲存行程，驗證「未登入使用者嘗試儲存」scenario 測試通過且登入後無需重新規劃
- [ ] 8.5 實作行程儲存與取回，驗證「取回內容一致」scenario 測試通過（站點、轉場、時間皆與儲存當下相同）
- [ ] 8.6 實作行程列出與刪除（含確認步驟），驗證列出、刪除、刪除前取消三個 scenario 測試通過
- [ ] 8.7 實作使用者資料隔離，驗證「存取他人行程」scenario 測試通過且回應不洩漏該行程是否存在

## 9. 介面與呈現

- [ ] 9.1 整合 MapLibre GL JS 與免費圖磚來源，驗證地圖可於頁面正常載入與平移縮放
- [ ] 9.2 實作行程結果的逐時段呈現，驗證每個站點顯示名稱、類別、地址、抵達與離開時間，且相鄰站點間顯示轉場
- [ ] 9.3 實作轉場的班次明細呈現，驗證公車分段顯示路線代號與站牌、步行分段顯示距離與時間
- [ ] 9.4 實作行程站點的周邊查詢入口，驗證「檢視行程站點周邊」scenario 於瀏覽器中表現正確且格式與獨立搜尋一致
- [ ] 9.5 實作行程與搜尋結果在地圖上的標示，驗證點選清單項目時地圖同步定位

## 10. 整合驗證與部署

- [ ] 10.1 以三組真實情境（台北三天兩夜、台南兩天一夜、澎湖三天兩夜）端到端執行規劃，驗證產出的行程中每一段轉場皆為真實可搭乘班次、且每個站點皆可回溯至 TDX 紀錄
- [ ] 10.2 量測單次規劃的 TDX 呼叫總數與總耗時，驗證未觸及 50 req/s 限制且落在 1.5 確認的函式執行時間上限內
- [ ] 10.3 驗證整份 specs 的所有 scenario 皆有對應測試且全數通過，列出測試與 scenario 的對照
- [ ] 10.4 部署至 Vercel 並以正式環境重跑 10.1 的三組情境，驗證結果與本地一致
